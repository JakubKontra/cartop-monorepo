{
  "installCommand": "corepack enable && corepack prepare yarn@stable --activate && yarn install",
  "buildCommand": "yarn workspace @cartop/validation build && yarn workspace @cartop/backend build",
  "version": 2,
  "builds": [{ "src": "apps/backend/api/index.ts", "use": "@vercel/node" }],
  "rewrites": [{ "source": "/(.*)", "destination": "/apps/backend/api/index" }]
}
