# Site URL (used for SEO and structured data)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Backend API URLs
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:3000/graphql

# Cache Revalidation
# This secret must match the CACHE_INVALIDATION_SECRET in the backend .env
# Used to authenticate webhook requests from the backend's Watch decorator
REVALIDATION_SECRET=dev-secret-change-in-production
